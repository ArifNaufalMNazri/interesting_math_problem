# Interesting_math_problem🧮
## The idea💡
This question came from a past math competition:

<i>The definition of a function `f` for any two positive integers `a` and `b` is defined as</i>

<i>f(a, b) = a is a = b, f(a-b, b) if a > b, f(b - a, a) if a < b. </i>

<i>Evaluate f(17, 9).</i>

## Python🐍 and C++
I made two versions of the program, one in **python** and one in **c++**. 

<p><img width="1351" height="501" alt="image" src="https://github.com/user-attachments/assets/52b797ba-3b1e-451a-95bf-e0c19410154b" />
</p>

## The solution🟰
Given two positive integers which are both **even**, the answer will always be **2**. If both are **odd** or **opposite parity**, the answer will be **1**. 

## My learning👁️
I'm learning python at the moment, so this program was a good learning project and refresher for my c++. I hope I can continue to make more programs like this in the future. 
